% Array to store which points are in a cluster and which are not
taken = zeros(num_points, 1);

for counter = 1:num_points
    % Ignoring already taken points
    if taken(counter,1) == 1
        continue;
    else
        % Clustering
        
    end
end